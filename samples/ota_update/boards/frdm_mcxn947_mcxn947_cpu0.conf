# MCX ROM-API flash HAL: backing store for the model registry and
# the A/B model slots in the bank 1 tail (see synaptic_partitions).
CONFIG_FLASH=y

# software reset for persistence / power-loss verification
CONFIG_REBOOT=y
